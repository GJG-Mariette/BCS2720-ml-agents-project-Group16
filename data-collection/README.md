# Data Collection Package 

The `data_collection` Python package is used in the combination of the modified
[ML-Agents Toolkit](https://github.com/Unity-Technologies/ml-agents).
`data_collection` provides a way to store data neccessary for the research


## Installation

Install the `data_collection` package with:

```sh
python -m pip install data_collection
```